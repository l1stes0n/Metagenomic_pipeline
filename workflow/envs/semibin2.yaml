channels: [conda-forge, bioconda, nodefaults]
dependencies:
  - python=3.11
  - semibin=2.5.0
  # Require the CUDA build rather than allowing a CPU-only PyTorch solution.
  - pytorch-gpu=2.5.1
  - cuda-version=12.0
  # NVIDIA drivers are supplied by the allocated cluster GPU node.
